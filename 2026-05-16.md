# 2026年05月16日

## 学んだこと
- **関数の基礎（function、引数、return）**
  - `return` 文を実行すると、関数はその時点で処理を終了し、外側に値を返す。
  - **キャメルケース（camelCase）**：2つ以上の単語を繋げて名前をつける際、2番目以降の単語の最初の文字を大文字にする書き方（可読性向上のため）。
    - スタートは必ず「小文字」（最初が大文字なのは「パスカルケース」という別のルール）。
    - JavaScriptでは変数名や関数名にキャメルケースを使うのが一般的。

- **関数の戻り値（return）の挙動**
  - 関数は、実行すると必ず何かしらの結果（戻り値）を返す。
  - `return` を書かない、または `console.log()` のみで処理を終えた関数は、外側に **`undefined`（空っぽ）** を返す。
  - そのため、`return` のない関数の実行結果を外側で別の数値と足し算（計算）しようとすると、`数字 + undefined` になり、結果が **`NaN`（Not a Number）** になる。

## 気づき
- **`return` と `console.log` の決定的な違い**
  - `return` は、次の処理に結果を「引き継ぐ」ことができる。
  - `console.log` は、その場で画面に「表示するだけ」で、次の処理に値は引き継げない（戻り値は `undefined` になる）。
  - 実験として、`return` のない関数を外側でさらに足し算するコードを動かし、意図通り `NaN` になる現象を確認できた。

- **構文のルールと発見**
  - `return` には `=`（代入）は不要。後ろにある値をそのまま外に「放り投げる」アクションであるため。
  - 異なる関数であれば、同じ引数名（`num1`, `num2` 等）を何度使い回しても問題ない。
    - **理由：** 「関数の中で作った引数や変数は、その関数（ボックス）の内部だけでしか生きられない」というスコープの鉄のルールがあるから。

- **関数名と変数の本質的な共通点**
  - 関数名とは、本質的には「関数（処理の塊）」という大きなデータが入っている変数の一種。
  - **カッコ `()` の有無による挙動の違い：**
    - カッコを **つける** `add()` 👉 中身の処理を **「実行」** する。
    - カッコを **つけない** `add` 👉 箱に入っている **「プログラムデータそのもの（数式）」** を指す。（そのため、カッコなしで `console.log` すると数式がそのまま表示される機能になっている）
