# 2026年05月31日
## 学んだこと
- **for文の応用課題**
  - `for`を使い、数値や配列の集計、配列の生成、オブジェクトのデータ取得など、複数の課題を解いた。
  - 配列を返す課題では、`result`用の配列を用意し、`push()`で要素を追加していくパターンを繰り返し使用した。
    - [前日](JavaScript/2026-05-30.md)学んだ「新しい配列を用意し、条件に合う要素をpushする」という考え方を複数の課題で活用できた。少しずつ定着してきたと感じる。
      
- **Math.round()**
  - 引数として与えた数値の小数点以下を四捨五入して整数を返す関数。
  - 配列内データの平均値を求める課題で利用した。  
    `result = Math.round( result / arr.length );`

## 気づき・詰まったこと
- **依然として、for文の継続条件と終了条件を混同しやすい**
  - for文の中央は「いつ終了するか」ではなく、「条件がtrueの間は続ける」という**継続条件**である。
  - 依然として終了条件を書いてしまう癖があるため、意図しない結果になった場合は、まず継続条件式を見直すこと。

- **「新しい配列を作る」考え方を再利用できた**
  - 新たな配列を作り、そこに`push()`する方法を自然に使えるようになってきた。  
    今後も時間を空けて復習し、定着させたい。
