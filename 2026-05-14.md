# 2026年05月14日

## 学んだこと
- 入校したスクールの課題を進めて、JSを初めて触りました。
  - console.log
- GitHubに、TILを作成！
  
## 気づき・詰まったこと
- jsに「1 + 1;」と書いた時に、何故コンソールに表示されないのかの理解。
  - ❌️最初は「そのまま文字列として表示されるのかな？」と思いましたが、コンソールに表示されなかったので「命令をしていないからそもそも実行されないんだ」という認識に至りました。
  - ⭕️AIに聞いたところ「出力の命令がない」から、とのこと……！計算はされているが、計算した結果を「画面に出せ」という命令（console.log）がないため、目に見えない形で処理が終了した、という回答。
- jsに「console.log(2 + 2);」と書いた時は、計算結果を画面に出すということは予想通りでした。
  - 計算した結果を画面に出す、という命令をしているため。
    - 「2 + 2」を「""」で囲むとどうなるかが気になり、試したところ、文字列として認識され「2 + 2」と出ました。
- 日本語を「""」で囲まない場合が気になったため、試したところエラー。
  - ただ、letで指定した場合（例：こんにちは = 1）は、console.log(こんにちは);とすると1が表示されました。
     - let を console.logの後に置くとエラー。
     - letで同じ変数を定義するのはNG。同じ名前で再定義したい場合は、letを使わず ◯◯ = ▼▼ と代入すればOK。
     - ただし、letではなく**constが推奨**される
