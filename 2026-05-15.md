# 2026年05月15日

## 学んだこと
- **データ型の基礎**
  - 基本的なデータ型（`string`, `number`, `boolean`, `undefined`）の理解。
  - `typeof` 演算子を用いた、実行時のデータ型確認方法。
- **変数と演算子**
  - 各演算子（四則演算、剰余 `%`、べき乗 `**`）の挙動。
  - 変数における「宣言」「代入」「定義」の厳密な違いの整理。

## 気づき・検証

### 1. 演算子における「暗黙の型変換」と評価順序
- 数値型と文字列型を `+` 演算子で結合した際の挙動（暗黙の型変換）を検証。
  - 原則として**左から順番に評価される**が、結合相手に文字列が存在する場合、数値が文字列へ変換されて結合される特性を確認。
  - `console.log(1 + 2 + "Hello");`  👉 先に数値の足し算が行われ、結果は `3Hello`
  - `console.log("Hello" + 1 + 2);`  👉 最初の結合で全体が文字列化し、結果は `Hello12`
  - `console.log("Hello" + (1 + 2));` 👉 カッコ内が最優先で評価され、結果は `Hello3`

### 2. 未定義値（undefined）の生成と宣言の仕様
- `typeof` で `undefined` を出力させるための条件を試行錯誤し、変数の仕様を整理。
  - `let` は値を持たせない「宣言のみ」が可能であり、自動的に `undefined` で初期化される。
  - 一方で `const` は、宣言と同時に値を代入する **「初期化」が必須**（SyntaxErrorとなる）である。

### 3. ブーリアン型における型変換の柔軟性
- `true` / `false` が、演算のコンテキスト（文脈）によって動的に評価を変える挙動を検証。
  - **算術演算の文脈：** `true` は `1`、`false` は `0` として扱われる（`1 + true` は `2` になる）。
  - **文字列結合の文脈：** 文字列の `"true"` / `"false"` として扱われる。
  - **考察（水の例え）：**
    コップ（数値の部屋）に入れれば「1」になり、お皿（文字の部屋）に入れれば「文字列」になる「水」のような柔軟性がある。しかし、成分そのものが書き換わったわけではないため、単体で `typeof` を評価すれば本来の姿である `boolean` を返す。JavaScriptの動的な型運用の仕組みが非常に腑に落ちた。
***
[▶ 2026年05月16日](2026-05-16.md)
