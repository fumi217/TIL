# 2026年05月15日

## 学んだこと
- データ型、演算子、変数の基礎
  - データ型：string / number / boolean
    - typeofを用いたデータ型の確認
  - 演算子：四則演算、余り、乗
  - 変数と宣言（let / const / var）・代入

## 気づき・詰まったこと
- なんとなくで理解した気になっていた「変数」、「宣言」、「代入」、「定義」について、AIにレクチャーしてもらいました！
  - 変数：私の回答…定義するもの → AI回答…データを入れておく、名前のついた箱
  - 宣言：let/constで変数を決めること（=の左側）
  - 代入：宣言した変数に、データを入れること
  - 定義：const sengen = "dainyu"　宣言して代入する、これが「定義」するということ。
- 数字と文字列の足し算が奥深い。
  - 文字列が優先される。
  - 左から処理される。
  - console.log(1 + 2 + "Hello");→3hello
  - console.log("Hello" + 1 + 2);→hello12
  - console.log("Hello" + (1 + 2));→hello3
- typeofで「undefined」を返すためにはどう記載したら良いのか？というのを試行錯誤しました。
  - ⭕️ const und1 = undefined;
  - ⭕️ let und2
    ❌️ ただし、= を書いてしまうと、エラーになる。宣言だけなら可。
  - ❌️ const und2
    　　「const」は、宣言のみは不可！
  - let は箱だけ置ける。const は必ず中身も一緒に入れる必要がある。
- ブーリアン型（true / false）の変身が奥深い。
  - 数値計算（+）の相手になると、true は「1」、false は「0」に変身して計算される（1 + true の結果は 2 になる）。
  - 文字列の相手になると、文字列の "true" に変身する。
  - 💡 イメージは「水」。
    コップ（算数の部屋）に入れれば数字の1になり、お皿（文字の部屋）に入れれば文字列になる。
    しかし成分自体は変わっていないため、単体で typeof で調べると本来の姿である boolean を返す（trueとあるが、文字列ではなくブーリアン！）。
    この概念が非常に腑に落ちた。
